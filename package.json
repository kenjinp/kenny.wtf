{
  "name": "kenny.wtf",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "pnpm --filter=astro dev",
    "build": "pnpm --filter=astro build",
    "format": "pnpm --filter=astro format",
    "format:check": "pnpm --filter=astro format:check",
    "lint": "pnpm --filter=astro lint",
    "lint:fix": "pnpm --filter=astro lint:fix",
    "ship": "pulumi up -C=./infra/pulumi -s=dev --yes",
    "preview": "pulumi preview -C=./infra/pulumi -s=dev"
  },
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@swc/core",
      "esbuild",
      "sharp"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sharp": "^0.33.1"
  }
}
